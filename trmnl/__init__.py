"""trmnl — an e-ink dashboard for a Seeed Studio TRMNL device.

A server builds one 800x480 1-bit screen (weather, Gdansk buses, Google
Calendar); the device fetches and displays it. The package is split along the
one architectural boundary the whole design rests on — see the sub-package
docstrings and plans/trmnl-dashboard/DESIGN.md (SS3.1).
"""
